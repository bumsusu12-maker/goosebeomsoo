# MONEYMAX fixed build

Run:
```bash
npm install
npm start
```

Open:
http://localhost:3001

Fixes applied:
- Restored broken Korean branch names in server and deployment API.
- Restored readable status/error messages.
- Kept server-side branch list and `functions/api/rates.js` branch list aligned.
- Verified JavaScript syntax checks for server and API files.
